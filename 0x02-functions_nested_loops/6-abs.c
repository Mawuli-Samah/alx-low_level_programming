#include "main.h"

/**
  * _abs - Computes the absolute value of an integer.
  * @n: The int to check.
  *
  * Return: Absolute value of number or zero
  */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}

